# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Runtime persistence (user data, regenerated)
rumpus-setup.json
rumpus-settings.json
rumpus-game.json

# Media / assets
# Photos, music and SFX are bundled and committed so the game works offline
# (see requirements/design_handoff_rumpus_golf/ASSETS.md for attribution).

# Fonts: bundled woff2 is committed (small, OFL); ad-hoc ttf/otf are ignored
web/assets/fonts/*.ttf
web/assets/fonts/*.otf

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
